# 参与贡献

Analog Studio 目前处于 V1.0 前的开发阶段。欢迎提交缺陷报告、设计讨论、文档和代码改进。

## 开始之前

1. 搜索现有 Issue，确认问题没有被重复记录。
2. 对行为变化或较大功能先创建 Issue，说明场景、预期结果和方案边界。
3. 从 `main` 创建短期分支，每个 Pull Request 聚焦一个问题。

## 本地检查

需要 Node.js 22.13.0 或更高版本。

```powershell
npm ci
npm run lint
npm test
```

`npm test` 会执行类型检查、生产构建和领域测试。修改数据库模型时，还应提交对应的 Drizzle 迁移和覆盖迁移路径的测试。

## 提交要求

- 说明问题、实现取舍、验证方式和仍存在的限制。
- 对缺陷修复添加可复现失败的测试；对新行为添加正常、边界和失败场景。
- 不要提交真实账号、令牌、数据库、日志、内部地址、个人目录或未经授权的第三方材料。
- 需求变更应新增版本化文件和变更记录，不直接覆盖冻结的 V1.0 需求源。
- 仅提交你有权公开和再分发的内容。

维护者会根据正确性、需求范围、测试证据和长期维护成本进行评审。Pull Request 的自动检查全部通过后才具备合并条件。
